[package]
name = "dcpdoctor-imf"
version = "0.2.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Shared IMF validation logic for dcpdoctor (no I/O dependencies)"

[dependencies]
quick-xml.workspace = true
dcpdoctor-parse.workspace = true
serde = { version = "1", features = ["derive"] }
